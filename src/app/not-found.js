import Error from "@/components/error/Error";

export const metadata = {
  title: 'Not Found || GigaStudios - Development Studio'
};

const Page = () => {
  return (
    <Error />
  );
};

export default Page;